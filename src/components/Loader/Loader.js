import React from 'react';
import { Loader } from 'semantic-ui-react';
import './Loader.css';

const CustomLoader = () => (
  <Loader active className='custom-loader' />
);

export default CustomLoader;
